package com.kentoes.rabbitmq.publisher;

//@RestController
//@RequestMapping("/rabbit/order")
public class OrderPublisher {
/*
    @Autowired
    RabbitTemplate template;

    @PostMapping("/{restaurantName}")
    public String bookOrder(@RequestBody Order order, @PathVariable String restaurantName) {
        order.setOrderId(UUID.randomUUID().toString());
        OrderStatus orderStatus = new OrderStatus(order, "PROCESSED", "order placed successfully in " + restaurantName);
        template.convertAndSend(RabbitConfig.EXCHANGE, RabbitConfig.ROUTINGKEY, orderStatus);
        return "Success !!";
    }
 */
}
